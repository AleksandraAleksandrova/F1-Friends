# Android notes

Use Android emulator for testing on Windows.

Minimum manual setup:
- Add `google-services.json` under `android/app/`.
- Run FlutterFire configuration and update `lib/firebase/firebase_config.dart`.
